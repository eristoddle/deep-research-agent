# Wanted modules

**Parked** 2026-08-21 under PLAN.md D2 — no new modules until a live project hits the gap.

Both are recorded in `ROADMAP.md`:

- **AI writing and prompting communities** — where practitioners compare model output: r/LocalLLaMA, r/WritingWithAI, Discord digests, practitioner blogs. Distinct from `general-web`, which is aimed at software best practices. The gap shows up when a question is "is this model any good at writing," which currently routes to bare `general-web`.
- **Docs and API reference** — a product's own documentation as a primary source, as distinct from `vendor-landscape`'s reading of its marketing. **Partly delivered 2026-08-29** by `agent-tooling`, which does this for agent tools. What remains is the same discipline for products outside that niche.

`AI writing and prompting communities` would extend the **AI ecosystem & market** family rather than sitting beside it as a near-duplicate. That assumption did **not** hold for the docs case: `agent-tooling` got its own **Tooling & platform** row, because its discriminating question (something you install and run, versus a model you call) is genuinely different. Test the question before assuming the family.

The trigger to unpark either: a real research project asks the question and `general-web` answers it badly. That failure is the evidence `/research-add-module` needs to do its discovery step honestly.
